# CRC Error Detection (Data Link Layer)

## Objective
Simulate cyclic redundancy check (CRC).

## What it does
Performs binary division to detect transmission errors.

## Key Insight
CRC is used in networking to detect corrupted data at the data link layer.
If the remainder is non-zero, an error is detected.
