# DNS Lookup (Application Layer)

## Objective
Resolve a domain name to an IP address.

## What it does
Uses system DNS to translate a human-readable domain into an IP.

## Key Insight
DNS is critical for the internet, allowing users to access websites without remembering IP addresses.
